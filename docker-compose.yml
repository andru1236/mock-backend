version: '3'

services:

  be-mock-api:
    build:
      context: .
    ports:
      - 5000:5000
    environment:
      PORT: 5000
      DB_NAME_MONGO: API_MOCK
      MONGO_CONNECTION: mongodb://mongo:27017/

  mongo:
    image: mongo
    volumes:
      - $HOME/mongo:/data/db
    ports:
      - 27017:27017
